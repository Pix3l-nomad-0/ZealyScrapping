[build]
  publish = "dist"
  functions = "netlify/functions"
  command = "npm run build"

[dev]
  framework = "vite"
  targetPort = 3000
  port = 8888

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200 